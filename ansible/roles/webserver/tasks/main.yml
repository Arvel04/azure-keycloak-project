---
- name: Ensure Nginx is installed
  apt:
    name: nginx
    state: present

- name: Start Nginx service
  service:
    name: nginx
    state: started
    enabled: yes

- name: Wait for Nginx to be ready
  wait_for:
    host: "localhost"
    port: 80
    state: started
    delay: 10
