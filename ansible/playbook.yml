- name: Set up container environment
  hosts: webserver
  become: true
  gather_facts: true
  vars:
    ansible_python_interpreter: /usr/bin/python3
  roles:
    - common
    - docker
    - keycloak
    - webserver
